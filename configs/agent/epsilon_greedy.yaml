# Epsilon-greedy agent configuration
agent:
  epsilon_greedy:
    epsilon: 0.1
    environment_type: bernoulli  # or gaussian
name: epsilon_greedy
n_arms: 5
decay:
  enabled: true
  type: exponential  # or 'linear'
  rate: 0.99         # Only used if enabled
  min_epsilon: 0.01
seed: 42 